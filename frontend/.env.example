# Frontend API base URL
# Local dev can keep this empty because Vite proxy maps /api to localhost:5000
# For production, set this to your backend URL + /api
# Example: https://toplis-api.onrender.com/api
VITE_API_URL=
